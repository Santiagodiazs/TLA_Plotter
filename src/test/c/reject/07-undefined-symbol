scene UndefinedSymbol {
    use casa {
        at (100, 100);
        scale 1.0;
        // Símbolo 'casa' no declarado
    }
    
    use arbol {
        at (200, 100);
        scale 1.5;
        // Símbolo 'arbol' no declarado
    }
}
